# 基础设施模块 (Infrastructure / :common)

## 核心职责
所有模块共享的基础组件：模块管理、依赖注入、通用工具、日志系统。

## 子目录
- `010-模块管理_ModuleManager/` — 模块生命周期管理
- `020-依赖注入_DI/` — Koin DI 配置
- `030-通用工具_Utils/` — 共享工具类
- `040-日志系统_Logging/` — 统一日志

## 关键约束
- 被所有其他模块依赖，修改时影响面最大
- 禁止引入对具体投屏协议的依赖（保持通用性）
- 修改此模块后必须确认所有依赖模块编译通过

## 对话结束选项

> 任务完成后，AI 必须调用 `ask_user_question` 从以下选项中选取 4 个最相关的：

| label | description |
|-------|-------------|
| 全量编译验证 | Gradle构建全部模块，确认修改未破坏任何依赖方 |
| 检查依赖模块 | 扫描app/mjpeg/rtsp/webrtc/input对common的调用是否兼容 |
| 继续完善组件 | 继续优化模块管理/DI/工具类/日志系统 |
| 更新文档提交 | 更新MODULES.md + git commit |
