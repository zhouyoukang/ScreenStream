# 一生二 项目规则入口

> 道生一，一生二，二生三，三生万物。为学日益，为道日损。无为而无不为。

## 规则体系 (道+器+术)

| 层 | 文件 | 加载 |
|----|------|------|
| **道** | Windsurf Settings > Global Rules | Always-On (五感×器+八德+本能层) |
| **器** | `rules/workspace.md` | Always-On (项目上下文+网络+硬约束) |
| **术** | `skills/` + `workflows/` | 按需 |

## 凭据中心
- **索引**: `凭据中心.md` + **实际值**: `secrets.env` (gitignored)
- 禁止将凭据写入Memory或git tracked文件
