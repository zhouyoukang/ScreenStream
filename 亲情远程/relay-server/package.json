{
  "name": "screenstream-relay",
  "version": "1.0.0",
  "description": "ScreenStream Public Network Relay Server - WebSocket video relay + control proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --dev"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
